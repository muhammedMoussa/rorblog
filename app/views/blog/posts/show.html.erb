<%= provide(:page_title, @post.title) %>
<%= provide(:blog_active, 'active') %>

<div class="row">
  <div>
    <h1><%= @post.title %></h1>
    <p><%= @post.publish_day %></p>
    <%= @post.body %>
  </div>
</div>

<%# <%= link_to 'Edit', edit_post_path(@post) %>
<%= link_to 'Back', posts_path %>
